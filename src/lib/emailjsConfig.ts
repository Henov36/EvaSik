/**
 * Заглушка конфигурации EmailJS. Значения ниже — плейсхолдери, їх треба
 * замінити реальними даними з кабінету emailjs.com (Email Services →
 * Service ID, Email Templates → Template ID, Account → General → Public Key).
 *
 * Поки тут стоять плейсхолдери, форма буде показувати повідомлення про
 * помилку замість "успішно відправлено" — це очікувано.
 */
export const EMAILJS_CONFIG = {
	serviceId: "YOUR_EMAILJS_SERVICE_ID",
	templateId: "YOUR_EMAILJS_TEMPLATE_ID",
	publicKey: "YOUR_EMAILJS_PUBLIC_KEY",
};
